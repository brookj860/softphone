[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["cp node_modules/@twilio/voice-sdk/dist/twilio.min.js public/js/twilio.min.js"]

[start]
cmd = "node server/index.js"
